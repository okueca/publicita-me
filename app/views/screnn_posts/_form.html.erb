<%= form_with(model: screnn_post) do |form| %>
  <% if screnn_post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(screnn_post.errors.count, "error") %> prohibited this screnn_post from being saved:</h2>

      <ul>
        <% screnn_post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :screen_id, style: "display: block" %>
    <%= form.number_field :screen_id %>
  </div>

  <div>
    <%= form.label :post_id, style: "display: block" %>
    <%= form.number_field :post_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
