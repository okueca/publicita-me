<%= stylesheet_link_tag('_navbar') %>

<nav class="navbar navbar-expand-lg bg-transparent">
    <a class="navbar-brand text-light ps-4" href="#">Navbar</a>
    <button class="navbar-toggler bg-light me-4" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end sm-bg-dark pe-5" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <%= link_to "Home",  homes_index_path, :class => "nav-link text-light" %>
        <a class="nav-link text-light" href="#">About</a>
       
        <% if current_user %>
          <%= link_to "Log out",  destroy_user_session_path, :class => "nav-link text-light" %>
        <% else %>
          <%= link_to "Sign in",  new_user_session_path, :class => "nav-link text-light" %>
          <%= link_to "Sign up",  new_user_registration_path, :class => "nav-link text-light" %>
          <a class="nav-link text-light" href="#">Perfil</a>
        <% end %>
      </div>
  </div>
</nav>